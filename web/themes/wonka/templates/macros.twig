{% macro field_media_image_url(field) %}
	{{ file_url(field[0]['#media'].field_media_image.entity.uri.value) }}
{% endmacro %}

{% macro field_media_svg_url(field) %}{{ file_url(field['#items'][0].entity.field_media_svg.entity.uri.value) }}{% endmacro %}

{% macro paragraph_field_media_unlimited_image_url(field) %}
	{{ file_url(field.entity.field_media_image.entity.uri.value) }}
{% endmacro %}

{% macro paragraph_field_media_video_external_url(field) %}
{{ field['#items'][0].entity }}
{% endmacro %}

{% macro paragraph_field_media_video_url(field) %}
{{ field['#items'][0].entity.field_media_video_file is not empty ? file_url(field['#items'][0].entity.field_media_video_file.entity.uri.value) : '' }}
{% endmacro %}

{% macro paragraph_field_media_image_url(field) %}{{file_url(field['#items'][0].entity.field_media_image.entity.uri.value)}}{% endmacro %}

{% macro destini_modal() %}
	<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
		<div class="modal-dialog modal-dialog-centered">
			<div class="modal-content">
				<div class="modal-header">
					<h5 class="modal-title" id="exampleModalLabel"></h5>
					<button type="button" class="close btn-close" data-bs-dismiss="modal" aria-label="Close">
						<span aria-hidden="true">×</span>
					</button>
				</div>
				<div class="modal-body">
					<div id="destinidiv" src=""></div>
				</div>
			</div>
		</div>
	</div>
{% endmacro %}

{% macro field_media_video_url(field)%}{{file_url(field['#items'][0].entity.field_media_video_file.entity.uri.value)}}{% endmacro %}